FROM python:3.12-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1

COPY pyproject.toml README.md ./
COPY src ./src
RUN pip install --no-cache-dir -e '.[api,live,monitoring]'

EXPOSE 8000
CMD ["uvicorn", "memos_q.api:app", "--host", "0.0.0.0", "--port", "8000"]
